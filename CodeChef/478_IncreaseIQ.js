function solve(input)
{
    let iq = Number(input)
        
        if( iq + 7 > 170 )
        {
            console.log("YES")
        }
        else
        {
            console.log("NO")
        }
    
}

// Boilerplate for JS
const fs = require("fs");

const input = fs.readFileSync(0, "utf8");

solve(input);







